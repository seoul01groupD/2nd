def process(commands):
    stack = []
    results = []
    
    for command in commands:
        if command == "push":
            pass
        elif command == "pop":
            if stack:
                pass